import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["d317fpcin2orjd.cloudfront.net", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
